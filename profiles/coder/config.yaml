# Coder — implementation worker. No secrets.
# Use a cheap/fast model unless the card pins a stronger one.

toolsets:
  - coding
  - kanban
  - memory
  - clarify

memory:
  memory_enabled: true

agent:
  verify_on_stop: true
