# Critic — judgment, not implementation. No secrets.

toolsets:
  - critic

custom_toolsets:
  critic:
    - kanban
    - memory
    - clarify
    - web
    - search
    - file
    - session_search

agent:
  disabled_toolsets:
    - terminal
    - coding
    - code_execution
    - computer_use
    - cronjob

memory:
  memory_enabled: true
